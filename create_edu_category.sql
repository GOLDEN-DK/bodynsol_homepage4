CREATE TABLE IF NOT EXISTS "edu_category" ("id" TEXT NOT NULL, "name" TEXT NOT NULL, "slug" TEXT NOT NULL, "description" TEXT, "isActive" BOOLEAN NOT NULL DEFAULT true, "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" TIMESTAMP(3) NOT NULL, CONSTRAINT "edu_category_pkey" PRIMARY KEY ("id"), CONSTRAINT "edu_category_name_key" UNIQUE ("name"), CONSTRAINT "edu_category_slug_key" UNIQUE ("slug"));
